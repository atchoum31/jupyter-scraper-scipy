FROM quay.io/jupyter/jupyter/scipy-notebook
CMD [ "/bin/bash -o pipefail -c mamba install --yes 'requests jupyterlab-lsp python-lsp-server[all]' && mamba clean --all -f -y && fix-permissions \"${CONDA_DIR}\" && fix-permissions \"/home/${NB_USER}\"" ]